import React from 'react';
import { Paper, Typography } from '@mui/material';

function BatchOperations() {
  return (
    <Paper sx={{ p: 2 }}>
      <Typography variant="h5">Batch Operations</Typography>
      <Typography>Batch operations interface coming soon...</Typography>
    </Paper>
  );
}

export default BatchOperations; 