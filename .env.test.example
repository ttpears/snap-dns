# Test Environment Configuration
# Copy this file to .env.test and customize for your environment

# Your hostname/IP for remote access (leave blank for localhost)
FRONTEND_HOST=foo.example.com
BACKEND_HOST=foo.example.com

# Frontend API URL (where the frontend will connect to the backend)
REACT_APP_API_URL=http://${BACKEND_HOST}:3002

# Backend CORS allowed origins (where requests can come from)
ALLOWED_ORIGINS=http://${FRONTEND_HOST}:3001

# Optional: Change ports if needed
# FRONTEND_PORT=3001
# BACKEND_PORT=3002
